Mean system: 1
Mean user: 1.004  у нескольких траекторий их больше одного user message
4 траекториях из 500 (примерно) был дополнительный user-message где-то посреди разговора.
Mean assistant: 32.892
Mean tool: 37.514
Claude 4.5 Opus в среднем делает на ~14% больше tool-вызовов, чем у него есть assistant-ходов. 
Это и есть параллельные tool_calls в действии
Mean total: 72.41 модель в среднем тратит 72 сообщения





print(f'n        : {len(totals)}')
print(f'min      : {totals[0]}')
print(f'p25      : {totals[len(totals)//4]}')
print(f'median   : {statistics.median(totals)}')
print(f'p75      : {totals[3*len(totals)//4]}')
print(f'p95      : {totals[int(len(totals)*0.95)]}')
print(f'max      : {totals[-1]}')
print(f'mean     : {statistics.mean(totals):.1f}')
print(f'stdev    : {statistics.stdev(totals):.1f}')

n        : 500
min      : 14
p25      : 48
median   : 63.0
p75      : 86
p95      : 147 5% траекторий имеют 147+ сообщений. Это либо очень сложные задачи, либо случаи, где модель "застряла" и долго бегала по кругу
max      : 222
mean     : 72.4
stdev    : 34.4